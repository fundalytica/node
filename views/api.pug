extends layout

block content
    main.d-flex.justify-content-center.text-center.p-4
        div.align-self-center
            h3.text-primary= text
            h1 👾
            p Available Endpoints
                ul.list-group.text-left
                each url in urls
                    li.list-group-item.text-left= url